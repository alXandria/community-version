import { ChainsService } from "../chains";
import { PermissionService } from "../permission";
import { Notification } from "./types";
export declare class BackgroundTxService {
    protected readonly notification: Notification;
    protected chainsService: ChainsService;
    permissionService: PermissionService;
    constructor(notification: Notification);
    init(chainsService: ChainsService, permissionService: PermissionService): void;
    sendTx(chainId: string, tx: unknown, mode: "async" | "sync" | "block"): Promise<Uint8Array>;
    private static processTxResultNotification;
    private static processTxErrorNotification;
}
