import { RNG } from "@keplr-wallet/crypto";
export declare type CoinTypeForChain = {
    [identifier: string]: number;
};
export declare type BIP44HDPath = {
    account: number;
    change: number;
    addressIndex: number;
};
export interface CommonCrypto {
    rng: RNG;
    scrypt: (text: string, params: ScryptParams) => Promise<Uint8Array>;
}
export interface ScryptParams {
    dklen: number;
    salt: string;
    n: number;
    r: number;
    p: number;
}
export interface ExportKeyRingData {
    type: "mnemonic" | "privateKey";
    key: string;
    coinTypeForChain: CoinTypeForChain;
    bip44HDPath: BIP44HDPath;
    meta: {
        [key: string]: string;
    };
}
