export declare class HasMapStore<T> {
    protected readonly creater: (key: string) => T;
    protected map: Map<string, T>;
    constructor(creater: (key: string) => T);
    protected get(key: string): T;
    protected has(key: string): boolean;
}
