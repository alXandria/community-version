import { Currency } from "@keplr-wallet/types";
import { CoinPrimitive } from "../types";
import { CoinPretty } from "@keplr-wallet/unit";
export declare class StoreUtils {
    static getBalancesFromCurrencies(currenciesMap: {
        [denom: string]: Currency;
    }, bals: CoinPrimitive[]): CoinPretty[];
    static getBalanceFromCurrency(currency: Currency, bals: CoinPrimitive[]): CoinPretty;
}
