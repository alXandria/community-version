import { ObservableQuery } from "../common";
import { KVStore } from "@keplr-wallet/common";
import { AxiosInstance } from "axios";
import { ChainGetter } from "../common";
import { HasMapStore } from "../common";
export declare class ObservableChainQueryRPC<T = unknown, E = unknown> extends ObservableQuery<T, E> {
    protected readonly _chainId: string;
    protected readonly chainGetter: ChainGetter;
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter, url: string);
    protected get instance(): AxiosInstance;
    get chainId(): string;
}
export declare class ObservableChainQueryRPCMap<T = unknown, E = unknown> extends HasMapStore<ObservableChainQueryRPC<T, E>> {
    protected readonly kvStore: KVStore;
    protected readonly chainId: string;
    protected readonly chainGetter: ChainGetter;
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter, creater: (key: string) => ObservableChainQueryRPC<T, E>);
}
