import { ObservableQuery } from "../../../common";
import { KVStore } from "@keplr-wallet/common";
export declare type SifchainLiquidityAPYResult = {
    rate: number;
};
export declare class ObservableQuerySifchainLiquidityAPY extends ObservableQuery<SifchainLiquidityAPYResult> {
    protected readonly chainId: string;
    constructor(kvStore: KVStore, chainId: string);
    protected canFetch(): boolean;
    get liquidityAPY(): number;
}
