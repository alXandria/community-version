import { SupplyTotal } from "./types";
import { KVStore } from "@keplr-wallet/common";
import { ObservableChainQuery, ObservableChainQueryMap } from "../../chain-query";
import { ChainGetter } from "../../../common";
export declare class ObservableChainQuerySupplyTotal extends ObservableChainQuery<SupplyTotal> {
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter, denom: string);
}
export declare class ObservableQuerySupplyTotal extends ObservableChainQueryMap<SupplyTotal> {
    protected readonly kvStore: KVStore;
    protected readonly chainId: string;
    protected readonly chainGetter: ChainGetter;
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter);
    getQueryDenom(denom: string): ObservableChainQuerySupplyTotal;
    getQueryStakeDenom(): ObservableChainQuerySupplyTotal;
}
