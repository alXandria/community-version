import { SpendableBalances } from "./types";
import { KVStore } from "@keplr-wallet/common";
import { ObservableChainQuery, ObservableChainQueryMap } from "../../chain-query";
import { ChainGetter } from "../../../common";
import { CoinPretty } from "@keplr-wallet/unit";
export declare class ObservableChainQuerySpendableBalances extends ObservableChainQuery<SpendableBalances> {
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter, address: string);
    get balances(): CoinPretty[];
}
export declare class ObservableQuerySpendableBalances extends ObservableChainQueryMap<SpendableBalances> {
    protected readonly kvStore: KVStore;
    protected readonly chainId: string;
    protected readonly chainGetter: ChainGetter;
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter);
    getQueryBech32Address(bech32Address: string): ObservableChainQuerySpendableBalances;
}
