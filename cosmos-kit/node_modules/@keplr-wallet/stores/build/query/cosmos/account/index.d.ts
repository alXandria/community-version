import { ObservableChainQuery, ObservableChainQueryMap } from "../../chain-query";
import { KVStore } from "@keplr-wallet/common";
import { ChainGetter } from "../../../common";
import { AuthAccount } from "./types";
export declare class ObservableQueryAccountInner extends ObservableChainQuery<AuthAccount> {
    protected readonly bech32Address: string;
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter, bech32Address: string);
    get sequence(): string;
    get isVestingAccount(): boolean;
}
export declare class ObservableQueryAccount extends ObservableChainQueryMap<AuthAccount> {
    protected readonly kvStore: KVStore;
    protected readonly chainId: string;
    protected readonly chainGetter: ChainGetter;
    constructor(kvStore: KVStore, chainId: string, chainGetter: ChainGetter);
    getQueryBech32Address(bech32Address: string): ObservableQueryAccountInner;
}
