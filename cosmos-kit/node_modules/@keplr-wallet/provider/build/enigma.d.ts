import { SecretUtils } from "secretjs/types/enigmautils";
import { Keplr } from "@keplr-wallet/types";
/**
 * KeplrEnigmaUtils duplicates the public methods that are supported on secretjs's EnigmaUtils class.
 */
export declare class KeplrEnigmaUtils implements SecretUtils {
    protected readonly chainId: string;
    protected readonly keplr: Keplr;
    constructor(chainId: string, keplr: Keplr);
    getPubkey(): Promise<Uint8Array>;
    getTxEncryptionKey(nonce: Uint8Array): Promise<Uint8Array>;
    encrypt(contractCodeHash: string, msg: object): Promise<Uint8Array>;
    decrypt(ciphertext: Uint8Array, nonce: Uint8Array): Promise<Uint8Array>;
}
